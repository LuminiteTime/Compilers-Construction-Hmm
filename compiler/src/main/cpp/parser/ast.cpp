#include "ast.h"

// Implementations for AST nodes if needed
// Most are simple structs, so no implementation required

// Destructors are defaulted in header file
// Add any additional implementations here if needed
